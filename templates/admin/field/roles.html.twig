{% block _User_roles %}
	{% dump %}
	{% for role in field.value %}
		{% if role == 'ROLE_ADMIN' %}
			Admin
		{% elseif role == 'ROLE_USER' %}
			Membre
		{% else %}
			Modérateur
		{% endif %}

        {#  {{ role }} : permet d'afficher le role au format ROLE_XXXX #}
	{% endfor %}

{% endblock %}
